# Personal AI tool configs (not meant for public repo)
.claude/
CLAUDE.md

# Per-project .gitignore handles its own node_modules / build output,
# but these are safety nets at the repo root.
node_modules/
.next/
out/
.expo/
web-build/
dist/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local env files (override per-project rules)
.env
.env.local
.env.*.local
